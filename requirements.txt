requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0          # faster/more forgiving HTML parser (falls back to stdlib if missing)
Pillow>=10.0       # image dimension check, used to drop thumbnails and icons

# Optional: only needed for --render (JavaScript-built shops)
# playwright>=1.44   then: playwright install chromium
